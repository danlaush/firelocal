{
  "name": "firelocal",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite example",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.51.2-build8"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
